import SwiftUI

extension Color {
    // Keep the app background color for backward compatibility, but use system colors in the new design
    static let appBackground = Color(.systemGroupedBackground)
}
